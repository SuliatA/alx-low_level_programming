#include <studio.h>

int main()
{
	for ( char c = 'A'; c <= 'Z'; c++ )

	{
		putchar( _ );
		putchar( p );
		putchar( u );
		putchar( t );
		putchar( c );
		putchar( h );
		putchar( a );
		putchar( r );
		putchar( \n );

	}

	return (0);

}
